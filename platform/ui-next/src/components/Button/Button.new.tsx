/**
 * NEW shadcn Button implementation
 *
 * This is a stub that re-exports the old implementation.
 * Replace the contents of this file with the new shadcn implementation when ready.
 *
 * The proxy (Button.tsx) will automatically use this when version='new'.
 */
export { Button, buttonVariants, type ButtonProps } from './Button.old';
